###
#   Felekidis Iosif 4062
###


# Phase 4-5


## How to run:

    make
    
    ./alphac < testfile <options>
        <options>:
            -s: print symbol table
            -q: print quad list
            -i: print instructions list
            -o: change the name of the binary file generated (default name is alpha.abc)    
    
    ./alpha <binary file> 
        <binary file>: if specified then execute it 
                       if not then it executes alpha.abc (default binary file name)


## Comments: 

    Ilopoihsa:  
        * ola ta basika;
        * ola ta library functions;
        * string concatination with '+' symbol --> "s1" + "s2" == "s1s2";
        * functor with dispatcher "()";

    den prosthesa epipleon table indexes pera apo ta num kai ta string.